[pytest]
# asyncio_mode=auto : les tests `async def` (anti-biais, repli ao_drafter)
# s'exécutent réellement sans décoration par test. Sans ça, une coroutine de test
# n'était jamais awaited (faux « vert »).
asyncio_mode = auto
testpaths = tests
