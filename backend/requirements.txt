# fastapi >= 0.115.6 pour tirer starlette >= 0.40 (CVE-2024-47874, DoS multipart)
fastapi==0.115.6
uvicorn[standard]==0.30.6
# >= 0.0.18 : corrige CVE-2024-53981 (DoS CPU via multipart malformé) — nos endpoints d'upload y sont exposés
python-multipart==0.0.20
python-jose[cryptography]==3.3.0
pdfplumber==0.11.4
python-docx==1.1.2
openpyxl==3.1.5
pymupdf==1.24.11
pytesseract==0.3.13
Pillow==10.4.0
openai==1.51.0
supabase==2.9.0
python-dotenv==1.0.1
httpx==0.27.2
boto3==1.35.49
pydantic==2.9.2
pydantic-settings==2.5.2
pydantic[email]
pyotp==2.9.0
qrcode==7.4.2
# Hachage des mots de passe de l'authentification maison (services/passwords.py),
# qui remplace Supabase Auth. Argon2id plutôt que bcrypt : bcrypt tronque
# silencieusement au-delà de 72 octets et son coût est purement CPU, donc
# parallélisable sur carte graphique. Argon2id impose un coût MÉMOIRE — 10 000
# essais simultanés exigent 190 Gio de RAM aux paramètres retenus.
argon2-cffi==23.1.0
